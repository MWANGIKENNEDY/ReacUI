import styled from 'styled-components';

export default styled.div`
    box-sizing:border-box;
    overflow-x:hidden;
    /*grid-template-columns:repeat(3,1fr)*/
    /*grid-auto-rows:minmax(200px,auto) - min width is 200 if empty - height auto depending on content*/
    height:100vh;
    width:100vw;
`;